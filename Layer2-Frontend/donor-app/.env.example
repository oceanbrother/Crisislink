VITE_SITE_PASSWORD=CrisisLink2026
# Local backend origin used by Vite proxy (/api and /static)
# Keep API calls proxied in dev; this prevents mixed-port regressions.
# VITE_BACKEND_ORIGIN=http://127.0.0.1:8000

# Optional: direct API URL for non-local environments
# VITE_API_URL=https://listing-service-production-78a8.up.railway.app

# Optional: frontend dev server port
# VITE_DEV_PORT=3004
